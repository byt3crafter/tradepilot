// SmartTradeInput — AI text-parsing removed; component retained as a stub
// so that DashboardHeader continues to compile.
import React from 'react';

const SmartTradeInput: React.FC = () => null;

export default SmartTradeInput;
