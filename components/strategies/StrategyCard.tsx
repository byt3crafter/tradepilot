import React from 'react';

// This component has been moved to components/playbooks/PlaybookCard.tsx
// This file is left to avoid breaking imports in older states but should be removed in the future.
const DeprecatedStrategyCard: React.FC = () => null;

export default DeprecatedStrategyCard;
