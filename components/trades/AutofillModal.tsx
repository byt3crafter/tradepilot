// AutofillModal — AI chart analysis removed; component stubbed.
import React from 'react';

interface AutofillModalProps {
  onClose: () => void;
  onApply?: (data: any) => void;
}

const AutofillModal: React.FC<AutofillModalProps> = ({ onClose: _onClose }) => null;

export default AutofillModal;
