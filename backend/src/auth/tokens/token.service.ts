import { Injectable } from '@nestjs/common';

@Injectable()
export class TokenService {
  // This service is deprecated after migration to Clerk.
  // Kept as a placeholder to avoid breaking module imports if any exist.
}
