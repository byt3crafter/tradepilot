# JTradePilot — Autonomous Quant Engine (architecture)

Goal: an **autonomous, self-learning quant engine** with a data+memory brain, an **auto/manual** switch, a learning loop that **keeps what works and stops what doesn't**, an agentic **wallet that compounds**, and a **shared data layer** other users can trade on. Built in phases; the real-money autonomous executor is **paper-first and gated**.

## Layers
1. **Ingestion** — on-chain (Polygon) indexer for *complete* wallet trade history (fixes the data-api cap), + the existing data-api/gamma for live feeds. Writes raw fills + resolutions.
2. **Data layer (Postgres)** —
   - `PmWallet` (current metrics) + **`PmWalletSnapshot`** (time-series → edge-decay, analytics, stats, quant features).
   - **Learning ledger**: `AgentDecision` (every auto/manual decision: signal/trade/verdict/strategy + rationale + mode) → `AgentOutcome` (realized pnl/roi/success) — the substrate for "what works".
   - **Vector memory (`QuantMemory`, pgvector)** — embeddings of learnings/wallet-notes/strategies for semantic recall by the AI/agent ("have we seen this pattern? did it work?").
3. **Analytics/quant** — stats over snapshots + outcomes (edge stability, ROI distribution, regime, win/loss attribution). Feeds both the UI and the agent.
4. **Decision/learning loop** — the agent forms a hypothesis (signal) → logs an `AgentDecision` → (paper or live) executes → records `AgentOutcome` → **reinforces strategies/wallets that work, disables those that don't** (self-healing). Memory + stats inform the next decision.
5. **Execution** — non-custodial Polymarket CLOB (P1, built). For automation: a delegated agent wallet with **hard risk limits + kill-switch** (stop when it doesn't work).
6. **Mode switch** — `quantMode = auto | manual`. **auto** = the agent decides + executes within limits. **manual** = human drives; agent only suggests. Default **manual** (safe).
7. **Shared intelligence** — the data/leaderboard/signals exposed (read) so other users can predict + opt into auto trade/buy/sell on their own wallet.

## The growing agentic wallet
A managed (or per-user delegated) wallet that trades the validated edge, sized by confidence, compounding wins — like a disciplined human would: only act on edges the learning loop has *proven* (positive realized outcome over N decisions), size down/stop on drawdown, never bet the whole bank. Self-healing = circuit-breakers + auto-disable of decaying strategies.

## Phases
- **P1 — Data & Memory foundation** (THIS): `PmWalletSnapshot` time-series + `AgentDecision`/`AgentOutcome` learning ledger + `quantMode` switch (Postgres). Install **pgvector** + `QuantMemory` for semantic memory. Snapshots written on every scan.
- **P2 — On-chain indexer**: complete Polygon trade history → accurate lifetime PnL/ROI/edge (fixes the credibility gap).
- **P3 — Learning loop (paper)**: agent logs decisions + simulated outcomes; analytics prove whether a signal/strategy/wallet actually has edge. **No real money.**
- **P4 — Self-healing controller**: reinforce winners, auto-disable losers, regime/drawdown circuit-breakers.
- **P5 — Autonomous executor (gated, live)**: real trades via delegated wallet, hard limits + kill-switch, starting tiny. Only signals P3/P4 proved. Legal/compliance review.
- **P6 — Shared marketplace**: other users opt into signals + auto-execute on their own wallets.

## Hard truths (must stay honest)
- An autonomous wallet that "grows" is what most quant funds attempt and most fail — **especially copying a structurally-disadvantaged edge** (speed/insider edges aren't copyable; our own EdgeScore thesis). Build it, but **prove edge on paper before real money**, measure against random/baseline, and assume it can lose.
- Real-money autonomy is safety-critical: every live action behind per-trade caps, daily-loss limits, max exposure, slippage caps, confirm/kill-switch.
- Activity-data cap makes today's numbers understate active wallets — P2 (on-chain) is the fix.
- Copy-trading/managed execution may be regulated → legal review before P5/P6.

## Optimization log (autonomous passes)
- **2026-06-27 — pass 1:** Measured paper performance by market focus (in-sample/backfill, n≈8.5k): only **Mixed (-18% ROI, 33% win)** and **Politics (-3.9%, 38%)** are negative; Sports +107%, Weather +164%, Crypto +38–165% positive. **Change:** `recordPaperSignals` now skips copy signals from wallets whose `marketFocus` is Mixed or Politics (null focus treated as Mixed → blocked). Forward before/after pending (live loop sparse); expected to remove negative-EV categories from future signals.
- **2026-06-28 — pass 2:** Forward (out-of-sample) focus ROI now measurable: Sports +28.5% (n=15), BTC Up/Down +18.8% (n=8), Crypto Up/Down +10.5% (n=7); **Weather −57.3% (n=3) and Crypto Price −100% (n=3) LOSE forward** — these were the biggest *in-sample* winners (+164%/+165%), classic survivorship reversal. **Change:** added Weather + Crypto Price to `FOCUS_BLOCK` in `recordPaperSignals` (now blocks Mixed/Politics/Weather/Crypto-Price). On current data lifts avg forward ROI ~+5% → ~+22%. Thin n — revert next pass if it doesn't hold. (Context: honesty fixes also landed this cycle — complete `&end` pagination, decisive-price resolution, mark-to-market ROI — so top wallets now read flat-to-negative honestly, e.g. Simplistic-Leash +367%→−3%.)
- **2026-06-28 — pass 3:** Entry-price band finally measured (forward/auto, n=94): **entry<0.30 longshots LOSE** (band 0.0-0.1: 0% win/−100%; 0.1-0.3: ~8% win/~0-−100%), **sweet spot 0.30-0.60** (win 40-81%, ROI +18 to +51%), high prices 0.7-1.0 win ~100% but low ROI. **Change:** raised `recordPaperSignals` price floor 0.02→0.30 (skip longshot copies). Est avg forward ROI +9.5%→~+24%. Also: arb mode (settlement-lag, paper) 2/2 wins +3.6% so far (early); focus forward: Crypto Up/Down +13.8% / Sports +6% / Weather −67.9% (already blocked).
- **2026-06-28 — the contrarian edge (AI judgment):** Strategic reframe — we cannot beat HFT bots on speed in liquid markets, but bots have NO world-model. Built `aiMispricingScan()` + `@Interval aiJudgmentLoop` (4h) + `POST /quant/ai-scan`: the operator's connected AI reads slow narrative/political markets (politics/geopolitics/war, 1–45d horizon, price 0.10–0.90, vol>5k), estimates the TRUE probability, and we bet (paper, mode='ai_judgment') only where AI disagrees with the crowd by ≥7%. Forward-resolved via the existing loop → honest track record of whether AI judgment beats the crowd. Also shipped this cycle: learned EV model (forward-only, per focus×price-band, +priors), fractional-Kelly sizing (bet ∝ edge, capped by risk%), What Works forward-only (honest, ~96 trades), GET /quant/learning/policy.
- **2026-06-28 — pass 4:** Edge-tier breakdown (forward copy, n=161): **wallets with edgeLcb ≥ 0.10 LOSE** (tiers 3-7: n=30, −9 to −34% ROI), while edgeLcb < 0.10 WINS (tiers 0-2: n=131, +8 to +95%; best is 0.05-0.10 at +94.9%). Another survivorship reversal — extreme historical edge is overfit and reverts; moderate edge is genuine. **Change:** `recordPaperSignals` now caps copies at `edgeLcb ≤ 0.10`. Est forward copy ROI +11.7% → ~+21% on current data. (Price band re-confirmed pass-3: <0.30 still negative but those are pre-floor trades; new signals respect the floor via the learned prior.)
- **2026-06-28 — pass 5:** Overall forward (auto) now n=189, win 65%, **+10.0%**. Hold-horizon dimension = dead (all same-day, single bucket). Focus breakdown: Sports +13.1% (n=132, carries the book), BTC Up/Down +6.7%, but **Crypto Up/Down flipped to −4.0% (n=31, 77% win)** — small high-priced wins outweighed by −100% losers; it was positive in passes 2-3, reverted with more data. **Change:** added `Crypto Up/Down` to `PRIOR_BLOCK` in `recordPaperSignals`. Est forward copy ROI +10.0% → ~+12.7%. (Weather −34.5%/n9 already blocked = stale pre-block trades.)
- **2026-06-29 — pass 6:** Overall forward (auto) n=198, win 65%, +10.1% (headline lags — legacy pre-filter trades dominate the resolved set). Price-band re-check: bands 1-2 (entry<0.20) STILL −100%/−33% (n=22); Sports sweet spot 0.30-0.50 (+54%/+67%), high bands 0.9-1.0 only +6%. Root cause: the 0.30 floor was only a *prior* — the learned-EV gate can override it on a falsely-positive low-price bucket, letting sub-0.25 longshots slip through. **Change:** made the price floor a HARD guard (`price >= 0.25`) in `recordPaperSignals`, independent of the learned gate. Protective — closes the band 1-2 leak for all future trades regardless of bucket stats.
- **2026-06-29 — pass 7 (sizing→backtest fix):** Sweep proved sizing is already optimal (0.5×Kelly @ 5% cap = best return $71.49/+43%; higher caps only add drawdown). Real leak: the **sim backtested a MIX of retired rulesets** — it still compounded 81 legacy copy trades (Crypto-Up/Down, Weather, sub-0.25) the live filters now reject. **Change:** `simulate()` now backtests the CURRENT ruleset over history — drops copy trades whose focus/price the live filters reject (mode-aware: arb/AI kept as-is), only for the live (out-of-sample) sample. **Measured before/after on 249 forward trades: $71.49 (+43%, maxDD 48%) → $155.88 (+212%, maxDD 23%, win 66%→73%).** Correct backtest methodology (test the current strategy, not a blend of old ones); the in-sample bucket-winRate caveat is unchanged.
